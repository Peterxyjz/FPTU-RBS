@model int

@if (Model > 0)
{
    <span class="badge bg-danger pending-bookings-count">@Model</span>
}
else
{
    <span class="badge bg-danger pending-bookings-count" style="display: none;">0</span>
}
